{
  "name": "sam-koa-server",
  "version": "0.0.2",
  "description": "Run lambdas locally without docker and from sam template.yaml",
  "keywords": [
    "sam",
    "lambda-local",
    "yaml",
    "server",
    "aws",
    "lambda"
  ],
  "main": "index.js",
  "scripts": {
    "test": "npx mocha --require test/require-for-test.js",
    "watch": "npx mocha --require test/require-for-test.js --watch",
    "start": "node index.js"
  },
  "bin": {
    "sam-koa-server": "./bin/sam-koa-server"
  },
  "author": {
    "name": "Gregor Tramsak"
  },
  "license": "ISC",
  "dependencies": {
    "command-line-args": "^5.1.0",
    "koa": "^2.7.0",
    "koa-bodyparser": "^4.2.1",
    "lambda-local": "^1.5.2",
    "yaml": "^1.4.0"
  },
  "devDependencies": {
    "debug": "^4.1.1",
    "dotenv": "^7.0.0",
    "mocha": "^6.0.2",
    "should": "^13.2.3"
  }
}
